import glob


scanned_songs = []
startbold = "\033[1m"
endbold = "\033[0;0m"
# add your mood key/tag/value


for file in glob.glob("/home/maxence/dev/banana/mix19-test/*.mp3"):
    scanned_songs.append(file)


for song in scanned_songs:
    ######audiofile = eyed3.load(song)
    #if hasattr(audiofile.tag, 'mood'):
    #    print(audiofile.tag.mood)
    #    continue
    print(song)
    print("What mood ?")
    print(startbold + "[S]" + endbold + "low, " + startbold + "[M]" + endbold + "edium, " + startbold + "[F]" + endbold + "ast")
    
    mood_choice = input()
    while mood_choice not in ["s", "m", "f"]:
        print("Wrong")
        mood_choice = input()

    if mood_choice == "s":  
        mood = "slow"
    elif mood_choice == "m":
        mood = "medium"
    elif mood_choice == "f":
        mood = "fast"
    #t = Tag()
    #t.title = "Gritty, yo!"
    #t.save("example-v2_4.id3", version=ID3_V2_4)
    # audiofile.initTag()
    # audiofile.tag.title = "10"    # example value
    # audiofile.tag.save()
    # print("You applied " + audiofile.tag.trasck_num  + " ¯\_(ツ)_/¯") # should print mood





